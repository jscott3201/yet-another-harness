{
  "$defs": {
    "ArtifactOffer": {
      "additionalProperties": false,
      "description": "A spilled result: the replier keeps the bytes and the caller pull-reads\nthem in bounded chunks, then must release the handle. The offer names\nsize and digest up front so the reader can refuse before the first pull,\nand verify after the last.",
      "properties": {
        "bytes": {
          "description": "At least one: a spill of zero bytes is not a spill, on either side.",
          "format": "uint64",
          "maximum": 9007199254740991,
          "minimum": 1,
          "type": "integer"
        },
        "digest_blake3": {
          "description": "BLAKE3 of the full content, hex. Whoever stores the artifact\ndurably mints its durable reference from bytes it hashed itself —\na wire offer is a claim, not provenance.",
          "pattern": "^[0-9a-f]{64}$",
          "type": "string"
        },
        "handle": {
          "$ref": "#/$defs/HandleId"
        },
        "media_type": {
          "maxLength": 128,
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "handle",
        "bytes",
        "media_type",
        "digest_blake3"
      ],
      "type": "object"
    },
    "CallId": {
      "description": "One call or one stream, numbered by the side that initiated it.\n\nWire form is a JSON number in `1..=2^53-1` so every SDK reads the same\nvalue; zero is reserved as never-valid. `number` on the TypeScript side,\nnot `bigint`: `JSON.parse` produces a `number`, and the I-JSON bound is\nexactly what makes that safe.",
      "format": "uint64",
      "maximum": 9007199254740991,
      "minimum": 1,
      "type": "integer"
    },
    "CancelReason": {
      "description": "Who ended a cancelled call.",
      "oneOf": [
        {
          "const": "requested",
          "description": "The caller asked.",
          "type": "string"
        },
        {
          "const": "enforced",
          "description": "The receiver's own enforcement ended it (deadline, shutdown).",
          "type": "string"
        }
      ]
    },
    "CancelTarget": {
      "description": "The two things a cancel can mean: stop working, or keep working but stop\nstreaming to me.",
      "enum": [
        "call",
        "stream"
      ],
      "type": "string"
    },
    "Ceilings": {
      "additionalProperties": false,
      "description": "Count ceilings the session enforces — the in-flight and live-handle\nceilings by refusal, a credit value past `max_stream_credit` as a\nfatal frame, and `initial_stream_credit` not at all: it is an\nannounced default, not a law.",
      "properties": {
        "host_calls_in_flight": {
          "description": "Host-initiated calls that may be in flight toward the worker.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "initial_stream_credit": {
          "description": "The opening credit window an SDK should grant when it has no\nbetter number. Advisory, not enforced: any opening grant in\n`1..=max_stream_credit` is legal.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "live_handles": {
          "description": "Live resource handles the worker may hold at once, capability and\nartifact alike — an artifact handle pins its bytes host-side, so an\nuncounted kind would be unbounded memory behind a bounded gauge.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "max_stream_credit": {
          "description": "Ceiling on one stream's outstanding credit — the unspent frames a\nproducer may hold at a moment, checked at open and at every\nwidening. Spent credit leaves the window, so grants over a stream's\nlifetime may sum past this.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "worker_calls_in_flight": {
          "description": "Worker-initiated calls that may be in flight toward the host.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "host_calls_in_flight",
        "worker_calls_in_flight",
        "live_handles",
        "initial_stream_credit",
        "max_stream_credit"
      ],
      "type": "object"
    },
    "HandleId": {
      "description": "A live resource named by its owner: a capability handle the host minted,\nor an artifact handle minted by whichever side holds the bytes.",
      "format": "uint64",
      "maximum": 9007199254740991,
      "minimum": 1,
      "type": "integer"
    },
    "HandleKind": {
      "description": "What a handle names.",
      "oneOf": [
        {
          "const": "capability",
          "description": "A brokered capability the host granted for this activation.",
          "type": "string"
        },
        {
          "const": "artifact",
          "description": "Spilled bytes held by one side for the other to pull-read.",
          "type": "string"
        }
      ]
    },
    "Outcome": {
      "description": "How a call ended, in exactly one of four ways. Cancelled is an outcome,\nnot an error — a call that was asked to stop and stopped did what it\nwas told. A completion racing a cancel is legal and the completion\nwins.",
      "oneOf": [
        {
          "additionalProperties": false,
          "description": "The result, inline. Domain failures ride here as data the method's\ncontract defines — a capability refusal is an answer, exactly as in\nthe Wasm lane — so the protocol error family stays closed and small.",
          "properties": {
            "outcome": {
              "const": "ok",
              "type": "string"
            },
            "result": true
          },
          "required": [
            "outcome",
            "result"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "The result was over the inline ceiling and spilled: the replier\nholds the bytes behind an artifact handle for the caller to\npull-read. Never a truncated inline result.",
          "properties": {
            "artifact": {
              "$ref": "#/$defs/ArtifactOffer"
            },
            "outcome": {
              "const": "spilled",
              "type": "string"
            }
          },
          "required": [
            "outcome",
            "artifact"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "The protocol boundary failed the call.",
          "properties": {
            "error": {
              "$ref": "#/$defs/WireError"
            },
            "outcome": {
              "const": "err",
              "type": "string"
            }
          },
          "required": [
            "outcome",
            "error"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "outcome": {
              "const": "cancelled",
              "type": "string"
            },
            "reason": {
              "$ref": "#/$defs/CancelReason"
            }
          },
          "required": [
            "outcome",
            "reason"
          ],
          "type": "object"
        }
      ]
    },
    "StreamClass": {
      "description": "The two delivery classes, shared vocabulary with the kernel's event\nlane: lossless data consumes credit and the producer must stop at zero;\nlossy data may be dropped oldest-first under pressure, counted.",
      "enum": [
        "lossless",
        "lossy"
      ],
      "type": "string"
    },
    "WireError": {
      "additionalProperties": false,
      "description": "The closed protocol-boundary error family, keyed by the caller's\nrecovery action. Domain failures never appear here — they are data on a\nsuccessful call — and messages carry no host path, registration id, or\ntype name, because an error that leaks internals is a tour a hostile\nworker did not have to ask for.",
      "properties": {
        "kind": {
          "$ref": "#/$defs/WireErrorKind"
        },
        "message": {
          "maxLength": 512,
          "type": "string"
        },
        "reconcile_required": {
          "description": "Whether the caller must reconcile external state before retrying —\nset on every outcome-unknown settlement.",
          "type": "boolean"
        },
        "retryable": {
          "description": "Whether the same request may be retried on this session under a\nfresh call id — the refused id is spent, so as-is resubmission is\nthe `duplicate-call` fault.",
          "type": "boolean"
        }
      },
      "required": [
        "kind",
        "message",
        "retryable",
        "reconcile_required"
      ],
      "type": "object"
    },
    "WireErrorKind": {
      "description": "Closed within a negotiated version. An unknown member is `invalid-frame`\nand the connection ends; evolution happens at the handshake version\ngate.",
      "oneOf": [
        {
          "const": "unsupported-version",
          "description": "No version in the hello is one the host speaks.",
          "type": "string"
        },
        {
          "const": "unknown-required-feature",
          "description": "A required feature is unknown to the receiving side.",
          "type": "string"
        },
        {
          "const": "negotiation-required",
          "description": "A frame arrived before negotiation completed, or a second hello.",
          "type": "string"
        },
        {
          "const": "invalid-frame",
          "description": "The frame failed admission — bad JSON, duplicate member names,\nnumbers outside the I-JSON safe range, an unknown tag or enum\nmember, or a field violating its bound — or an admitted frame broke\nstream order or shape: data before open or after the last item, an\nopen on a call that did not ask to stream, a second open, a\nsequence gap, a credit value of zero or past the ceiling, a drop\ncount going backwards, a zero-byte spilled offer. Fatal everywhere\nexcept one place: a malformed payload on a served `artifact.read`\ncall is refused on that call alone.",
          "type": "string"
        },
        {
          "const": "frame-too-large",
          "description": "The length prefix exceeded the frame class's byte bound.",
          "type": "string"
        },
        {
          "const": "payload-too-large",
          "description": "A payload exceeded its class bound inside a well-formed frame.",
          "type": "string"
        },
        {
          "const": "unknown-call",
          "description": "A reply, stream-open, or stream-data frame named a call id never\nminted in that direction. Late frames for an id that already settled\nare tolerated as races, and a cancel or credit naming an unknown id\nis ignored — those cross terminals on the wire in ordinary use.",
          "type": "string"
        },
        {
          "const": "duplicate-call",
          "description": "A call id already spent in the same direction — in flight or already\nanswered — was reused.",
          "type": "string"
        },
        {
          "const": "unknown-method",
          "description": "The receiver does not offer the named method.",
          "type": "string"
        },
        {
          "const": "resource-exhausted",
          "description": "An in-flight ceiling was hit — refused rather than queued, and worth\nretrying once something settles. The live-handle ceiling never\nreaches the wire as this kind: the host hands that refusal to its\napplication, which answers in the capability family's own\nvocabulary. A lossless producer overdrawing its credit window is\nalso this kind, but as a fatal fault: the window was announced, so\nexceeding it is a broken producer, not congestion.",
          "type": "string"
        },
        {
          "const": "deadline-exceeded",
          "description": "The call's millisecond budget expired; the host is the enforcer.",
          "type": "string"
        },
        {
          "const": "cancelled",
          "description": "The session ended in an orderly goodbye before this call's terminal\nframe arrived. Distinct from a worker terminal with\n[`Outcome::Cancelled`], which is the call itself answering.",
          "type": "string"
        },
        {
          "const": "unknown-handle",
          "description": "A handle desync: a release, read, or use named a handle the owner\ndoes not hold, a release or release-ack named the wrong kind, an\nack answered a release that was never sent, or an offer reused a\nhandle id — spent or still live, ids are minted fresh forever.",
          "type": "string"
        },
        {
          "const": "invalid-read",
          "description": "An artifact pull-read went outside the offered byte range, over\nthe per-read bound, or asked for zero bytes.",
          "type": "string"
        },
        {
          "const": "outcome-unknown",
          "description": "The peer is gone and the outcome of in-flight work is unknowable\nfrom this side. Reconciliation is required before unsafe repetition.",
          "type": "string"
        },
        {
          "const": "internal",
          "description": "The receiving side failed internally; details stay on its side.",
          "type": "string"
        }
      ]
    },
    "WireLimits": {
      "additionalProperties": false,
      "description": "Byte bounds the session enforces per frame class, echoed to the worker\nso both sides refuse at the same line.",
      "properties": {
        "max_artifact_read_bytes": {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "max_call_payload_bytes": {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "max_control_frame_bytes": {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "max_frame_bytes": {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "max_inline_result_bytes": {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "max_stream_data_bytes": {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "max_frame_bytes",
        "max_control_frame_bytes",
        "max_call_payload_bytes",
        "max_inline_result_bytes",
        "max_stream_data_bytes",
        "max_artifact_read_bytes"
      ],
      "type": "object"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Everything a host may put on the wire.",
  "oneOf": [
    {
      "additionalProperties": false,
      "description": "Terminal answer to a hello the host can serve.",
      "properties": {
        "ceilings": {
          "$ref": "#/$defs/Ceilings"
        },
        "features": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "frame": {
          "const": "accept",
          "type": "string"
        },
        "limits": {
          "$ref": "#/$defs/WireLimits"
        },
        "protocol_version": {
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "frame",
        "protocol_version",
        "features",
        "limits",
        "ceilings"
      ],
      "type": "object"
    },
    {
      "additionalProperties": false,
      "description": "Terminal answer to a hello the host cannot serve; the connection\nends after this frame. Names every version the host does speak, so\nthe worker fails with a diagnostic instead of a bare close.",
      "properties": {
        "error": {
          "$ref": "#/$defs/WireError"
        },
        "frame": {
          "const": "refuse",
          "type": "string"
        },
        "supported_versions": {
          "items": {
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "type": "array"
        }
      },
      "required": [
        "frame",
        "error",
        "supported_versions"
      ],
      "type": "object"
    },
    {
      "additionalProperties": false,
      "description": "One request. The initiator mints the id; the receiver answers with\nexactly one terminal [`Reply`] carrying the same id, whatever else\nhappens — cancellation included.",
      "properties": {
        "call_id": {
          "$ref": "#/$defs/CallId"
        },
        "deadline_ms": {
          "description": "Relative millisecond budget, not an instant: a sandboxed worker that\nhas been denied clock access can still count down, and the host\nenforces the deadline regardless of what the worker does.\n`nullable` because serde writes an absent budget as `null` — the\nTypeScript type must admit the value actually on the wire.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "frame": {
          "const": "call",
          "type": "string"
        },
        "method": {
          "maxLength": 128,
          "minLength": 1,
          "type": "string"
        },
        "payload": true,
        "stream": {
          "description": "Whether the caller wants incremental results. A receiver that agrees\nanswers with [`StreamOpen`] before any data; the terminal reply still\nends the call either way.",
          "type": "boolean"
        }
      },
      "required": [
        "frame",
        "call_id",
        "method",
        "stream",
        "payload"
      ],
      "type": "object"
    },
    {
      "additionalProperties": false,
      "description": "The single terminal frame of a call.",
      "properties": {
        "call_id": {
          "$ref": "#/$defs/CallId"
        },
        "frame": {
          "const": "reply",
          "type": "string"
        },
        "outcome": {
          "$ref": "#/$defs/Outcome"
        }
      },
      "required": [
        "frame",
        "call_id",
        "outcome"
      ],
      "type": "object"
    },
    {
      "additionalProperties": false,
      "description": "Acknowledges a `stream: true` call and grants initial credit. Mandatory\nbefore any data frame, so \"did this stream ever start\" is decidable.",
      "properties": {
        "call_id": {
          "$ref": "#/$defs/CallId"
        },
        "credit": {
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "frame": {
          "const": "stream-open",
          "type": "string"
        }
      },
      "required": [
        "frame",
        "call_id",
        "credit"
      ],
      "type": "object"
    },
    {
      "additionalProperties": false,
      "description": "One incremental item on a stream. `seq` is monotonic from zero per\nstream; `more: false` marks the last data frame, after which only the\nterminal reply may follow — end-of-stream is decidable frame by frame,\nwithout a timeout.",
      "properties": {
        "call_id": {
          "$ref": "#/$defs/CallId"
        },
        "class": {
          "$ref": "#/$defs/StreamClass"
        },
        "dropped": {
          "description": "Cumulative count of lossy frames dropped before this one, so a\nconsumer can see the gap it was spared rather than infer it.",
          "format": "uint64",
          "maximum": 9007199254740991,
          "minimum": 0,
          "type": "integer"
        },
        "frame": {
          "const": "stream-data",
          "type": "string"
        },
        "more": {
          "type": "boolean"
        },
        "payload": true,
        "seq": {
          "format": "uint64",
          "maximum": 9007199254740991,
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "frame",
        "call_id",
        "seq",
        "more",
        "class",
        "dropped",
        "payload"
      ],
      "type": "object"
    },
    {
      "additionalProperties": false,
      "description": "Grants the producer more frames of credit on one stream. Credit is the\nwire contract for backpressure: in-process advisories like `drain()` are\nsuggestions a hostile producer ignores, a spent window is a rule the\nreceiver enforces.",
      "properties": {
        "additional": {
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "call_id": {
          "$ref": "#/$defs/CallId"
        },
        "frame": {
          "const": "credit",
          "type": "string"
        }
      },
      "required": [
        "frame",
        "call_id",
        "additional"
      ],
      "type": "object"
    },
    {
      "additionalProperties": false,
      "description": "Ask the receiver to stop. Advisory by nature — Node and CPython can only\nhonor it at cooperative points — which is why the call still owes its\nterminal frame and the host keeps its own deadline enforcement.",
      "properties": {
        "call_id": {
          "$ref": "#/$defs/CallId"
        },
        "frame": {
          "const": "cancel",
          "type": "string"
        },
        "target": {
          "$ref": "#/$defs/CancelTarget"
        }
      },
      "required": [
        "frame",
        "call_id",
        "target"
      ],
      "type": "object"
    },
    {
      "additionalProperties": false,
      "description": "Release a handle to its owner. Explicit and acknowledged: finalizers do\nnot run reliably in any lane this protocol serves, so release is a\ntwo-party fact, not a hope.",
      "properties": {
        "frame": {
          "const": "release",
          "type": "string"
        },
        "handle": {
          "$ref": "#/$defs/HandleId"
        },
        "kind": {
          "$ref": "#/$defs/HandleKind"
        }
      },
      "required": [
        "frame",
        "handle",
        "kind"
      ],
      "type": "object"
    },
    {
      "additionalProperties": false,
      "description": "The owner confirms the handle is gone and its id may never be reused\nwithin the session — the acknowledgement closes the release/reuse race.",
      "properties": {
        "frame": {
          "const": "release-ack",
          "type": "string"
        },
        "handle": {
          "$ref": "#/$defs/HandleId"
        },
        "kind": {
          "$ref": "#/$defs/HandleKind"
        }
      },
      "required": [
        "frame",
        "handle",
        "kind"
      ],
      "type": "object"
    },
    {
      "additionalProperties": false,
      "description": "Orderly shutdown notice; either side may send it.",
      "properties": {
        "frame": {
          "const": "goodbye",
          "type": "string"
        },
        "reason": {
          "maxLength": 256,
          "type": "string"
        }
      },
      "required": [
        "frame",
        "reason"
      ],
      "type": "object"
    }
  ],
  "title": "HostMessage"
}
